# The toolchain this site is built with.
#
# CI used to run a bare `pip install mkdocs-material`, so every deploy built
# with whatever was current.  That is how 54 display-math blocks that had
# never been well formed stayed invisible from 2009 until mkdocs-material
# 9.7.7 stopped rescuing them, and published 108 stray dollar signs across
# 17 pages without a line of content changing (issue #2).
#
# Pinned at the versions the 2026-09-13 deploy used, so upgrading is a
# deliberate act with a diff to read rather than a surprise on push.

babel==2.18.0
backrefs==8.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
ghp-import==2.1.0
idna==3.19
Jinja2==3.1.6
Markdown==3.10.3
MarkupSafe==3.0.3
mergedeep==1.3.4
mkdocs==1.6.1
mkdocs-get-deps==0.2.2
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
packaging==26.3
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.11.8
Pygments==2.21.0
pymdown-extensions==11.0.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
pyyaml_env_tag==1.1
requests==2.34.2
six==1.17.0
urllib3==2.7.0
watchdog==6.0.0
